const db = require("../knex.config");

const table = "users";

const find = () => {
  return null;
};

const findById = id => {
  return null;
};

const findByUsername = username => {
  return null;
};

const update = (id, changes) => {
  return null;
};

const add = model => {
  return null;
};

const remove = id => {
  return null;
};

module.exports = {
  find,
  findById,
  findByUsername,
  update,
  add,
  remove
};
